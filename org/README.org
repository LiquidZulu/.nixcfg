#+title: Readme!

The files in =.nixcfg/org= need to be manually tangled, to do this
place the point over the =#+PROPERTY:= tag and run =org-ctrl-c-ctrl-c=
(=C-c C-c=), then =org-babel-tangle= (=C-c C-v t=).
